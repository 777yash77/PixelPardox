# Server Configuration (Render dynamically assigns $PORT)
server.port=${PORT:8080}

# Database Configuration (Supports Render PostgreSQL or MySQL)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/pixel_paradox?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Yash300303#}
spring.datasource.driver-class-name=${SPRING_DATASOURCE_DRIVER_CLASS_NAME:com.mysql.cj.jdbc.Driver}

# JPA / Hibernate Configuration (Hibernate 6 auto-detects dialect based on JDBC driver/URL)
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql=false
spring.jpa.open-in-view=false

# File Upload Configuration
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB

# Mail/SMTP Configuration (Gmail - Secure STARTTLS)
# IMPORTANT: Use a Gmail App Password (not your regular password)
# Generate at: https://myaccount.google.com/apppasswords (2FA must be enabled)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=ywanth224@gmail.com
spring.mail.password=cjgrcufqpvkdbacc
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.debug=false

# JWT Configuration
app.jwt.secret=${JWT_SECRET:d39352e85a6a68456a0845a7092bb45e3f898394ef3493df54f15d74c0c1b7e5}
# 1 day expiration (86,400,000 milliseconds)
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}
